[package]
name = "wasm-executor-server"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["full"] }
tokio = { version = "1", features = ["full"] }
# より安定したバージョンを使用
wasmtime = "15.0"
wasmtime-wasi = "15.0"
